## Lessons

- The old instruction set’s main weakness was not just length, but ownership ambiguity: commits, tests, and fixes were spread across layers.
- Reviewer autonomy works well when bounded by a hard test boundary: “fix directly if no new tests are needed; otherwise escalate.”
- Concision improved once the files were separated by purpose: global policy, delegation playbook, and per-agent role behavior.
